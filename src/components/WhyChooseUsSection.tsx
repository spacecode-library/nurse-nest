
import { useIsMobile } from '@/hooks/use-mobile';
import { cn } from '@/lib/utils';

export default function WhyChooseUsSection() {
  const isMobile = useIsMobile();

  // Return an empty fragment to effectively remove this section
  return null;
}
